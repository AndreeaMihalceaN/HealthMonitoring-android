<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.andreea.healthmonitoring">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature
        android:name="android.hardware.sensor.stepcounter"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.sensor.stepdetector"
        android:required="false" />

    <!-- <uses-feature android:name="android.hardware.sensor.stepcounter" /> -->
    <!-- <uses-feature android:name="android.hardware.sensor.stepdetector" /> -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeActivity"
            android:label="Home" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".TestActivity" />
        <activity
            android:name=".FoodDiaryActivity"
            android:label="Food Diary" />
        <activity
            android:name=".ProfileActivity"
            android:label="Profile" />
        <activity
            android:name=".AddFoodActivity"
            android:label="Add Food" />
        <activity android:name=".MonitoringWaterActivity" />
        <activity android:name=".WalkingActivity" />
        <activity android:name=".InitialProfileActivity" />
        <activity android:name=".Walking2Activity" />
        <activity android:name=".BmiActivity" />
        <activity android:name=".NotificationView" />
        <!--<activity android:name=".FoodSugestionActivity"></activity>-->
        <activity
            android:name=".FoodSugestionActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>